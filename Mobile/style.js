import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  preencher: {
    flex: 1,
  }
})

export const colors = {
  background: '#FFF',
  primary: '#3dcedb',
  accent: '#ebae34',
  danger: '#Ed1c24',
}